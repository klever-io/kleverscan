import { Container } from '@/views/transactions';
import React from 'react';

const Transactions: React.FC = () => {
  return (
    <Container>
      <h1>Transactions</h1>
    </Container>
  );
};

export default Transactions;
